"""Relational fraud intelligence.

Library code for the pipeline. Entrypoints live in ``scripts/`` and contain no
analytical logic — tests import this package and never the scripts, so anything
that lives in a script is untested by construction.
"""

__version__ = "0.1.0"
